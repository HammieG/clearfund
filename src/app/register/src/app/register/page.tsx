"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
export default function RegisterPage() { const router = useRouter(); const [error, setError] = useState(""); const [pending, setPending] = useState(false); async function submit(data: FormData) { setPending(true); setError(""); try { const response = await fetch("/api/register", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(data)) }); const body = await response.json().catch(() => ({})); if (!response.ok) setError(body.error || "We could not create your account. Please try again."); else router.push("/signin?registered=1"); } catch { setError("We could not reach the service. Please try again."); } finally { setPending(false); } } return <div className="mx-auto mt-14 max-w-md card p-7"><h1 className="text-2xl font-bold">Create your donor account</h1><p className="mt-2 text-slate-600">Pledge securely, then follow verification in one place.</p><form action={submit} className="mt-6 space-y-4"><input className="input" name="name" placeholder="Your name" autoComplete="name" required/><input className="input" name="email" type="email" placeholder="Email" autoComplete="email" required/><input className="input" name="password" type="password" minLength={8} placeholder="Password (8+ characters)" autoComplete="new-password" required/><button className="button w-full" disabled={pending}>{pending ? "Creating…" : "Create account"}</button></form>{error && <p className="mt-3 text-sm text-red-700" role="alert">{error}</p>}<p className="mt-5 text-sm text-slate-600">Already registered? <Link className="font-semibold text-blue-800" href="/signin">Sign in</Link></p></div>; }
